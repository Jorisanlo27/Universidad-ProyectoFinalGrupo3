@model IEnumerable<Universidad.Models.Estudiante>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdCarreraNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdPersonaNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdCarreraNavigation.IdCarrera)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdPersonaNavigation.IdPersona)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdEstudiantes">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdEstudiantes">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdEstudiantes">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
